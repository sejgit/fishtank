#!/bin/bash
# bootstrap
# don't forget to bring in aio.key which is in .ssh
# don't forget to install paul which is in dotfiles

sudo apt install python3-pip python3-rpi.gpio python3-schedule
sudo pip3 install Adafruit_IO

